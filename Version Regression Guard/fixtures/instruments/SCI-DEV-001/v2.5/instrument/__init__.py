"""SCI-DEV-001 instrument control software (chromatograph / pressure analyzer).

This is a MOCK instrument codebase used by AI Kavach to demo
software-update validation. Two versions live side by side (v2.4 baseline,
v2.5 candidate) so the pipeline can diff, impact-analyze, and A/B test them
inside the digital twin. Nothing here talks to real hardware.
"""

__version__ = "2.5"
